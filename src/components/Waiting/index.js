import React, { Component } from 'react';
import classnames from 'classnames';

export default class Waiting extends Component {
  render () {
    return (
      <div className="scene__waiting">
        Waiting...
      </div>
    );
  }
}